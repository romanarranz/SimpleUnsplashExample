extends base

block main
	.ui.main.container
		form.ui.grid(method="post" action="/unsplash/save_token")
			.row
				p Introduce el token que aparece en la cabecera
			.row
				.ui.input
					input#unsplash_bearer_token.validate(type="text" name="unsplash_bearer_token" placeholder="Bearer Token")
			.row
				button.ui.right.labeled.icon.button(type="submit") #[i.save.right.icon] Save
